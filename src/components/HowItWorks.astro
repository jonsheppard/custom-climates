---
const steps = [
    {
        number: '01',
        title: 'Book Your Consultation',
        description: 'Tell us your materials, location, & timeline—it starts with a 5-minute call or a quick online form.'
    },
    {
        number: '02',
        title: 'Choose Unit & Dates',
        description: 'Trailer or conex, size & spec—we lock in your delivery, quote, and a custom monitoring plan.'
    },
    {
        number: '03',
        title: 'We Deliver & Monitor',
        description: "Your unit rolls in, powers up, and streams live data. Our team babysits the climate so you don't have to."
    }
]
---

<section class="py-20">
    <div class="container mx-auto px-6">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold font-heading text-neutral-dark">From Phone Call to Climate Control in Three Simple Moves</h2>
            <p class="mt-4 text-lg text-neutral-medium max-w-2xl mx-auto">Ready, set, ship. We make it that easy.</p>
        </div>
        <div class="relative">
            <!-- Dotted line for desktop -->
            <div class="hidden md:block absolute top-1/2 left-0 w-full h-0.5 border-t-2 border-dashed border-neutral-medium" style="transform: translateY(-50%);"></div>

            <div class="relative grid md:grid-cols-3 gap-12">
                {steps.map(step => (
                    <div class="text-center relative bg-neutral-light p-8 rounded-lg shadow-lg">
                        <div class="flex items-center justify-center w-20 h-20 mx-auto mb-6 bg-brand-blue text-white rounded-full text-3xl font-bold font-heading">
                            {step.number}
                        </div>
                        <h3 class="text-2xl font-bold font-heading">{step.title}</h3>
                        <p class="mt-2 text-neutral-medium">{step.description}</p>
                    </div>
                ))}
            </div>
        </div>
    </div>
</section> 